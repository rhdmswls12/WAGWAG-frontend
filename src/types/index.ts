/**
 * type 중앙화를 위한 디렉토리입니다.
 *
 * /core : 기본 타입 정의
 * - response, error 등 기본 인터페이스 type/class 정의
 *
 * /entities : 엔티티 타입 정의
 * - 기본 엔티티 타입 정의 ex) User
 *
 * /schemas : 스키마 타입 정의
 * - POST/PUT/PATCH 요청의 body에 해당하는 타입 정의
 *
 * /params : 파라미터 타입 정의
 * - GET 요청의 query 파라미터 타입 정의
 *
 * /enums : enum 및 파생 타입 정의
 *
 */
